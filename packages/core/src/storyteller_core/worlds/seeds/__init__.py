"""Seed-world JSON resources, one file per (id, locale).

Loaded at import time by ../seed.py — see that module for the public
API (`seed_worlds`, `SEED_WORLDS`, `write_seed`). To regenerate after
a schema change run `scripts/augment_seed_worlds.py`.
"""
